# PCam website agent instructions

**`CLAUDE.md` is the single authoritative instruction file for this
repository. This file intentionally holds no rules of its own.**

Read [CLAUDE.md](./CLAUDE.md) and follow it in full — operating model,
architecture, content rules, media rules, safety, and git discipline.

@CLAUDE.md

---

Why the indirection: these two files were previously byte-for-byte duplicates,
which meant every rule change had to be made twice and any missed edit left two
disagreeing sources of truth. Keeping the content in one place removes that
drift. Add new rules to `CLAUDE.md` only.
